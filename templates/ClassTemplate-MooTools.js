AYB.namespace("AYB.collection");

AYB.collection.ClassName = new Class({
    Implements: [Options, Events],
    options: {
    },
    initialize: function(options) {
        this.setOptions(options);
    },

    findElements: function() {
    },
    addListeners: function() {
    }
});